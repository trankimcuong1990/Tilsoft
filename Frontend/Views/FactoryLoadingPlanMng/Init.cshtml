@{
    ViewBag.Title = "Create new Loading Plan";
    ViewBag.Module = "Loading Plan";
}

<form class="row" name="editForm">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <div class="jarviswidget" id="wid-id-general" data-widget-editbutton="false" data-widget-colorbutton="false" data-widget-fullscreenbutton="false" data-widget-deletebutton="false">
            <header>
                <h2>Select item to create PLC</h2>
            </header>
            <div>
                <div class="widget-body no-padding">
                    <div class="smart-form">
                        <fieldset>
                            <section>
                                <label class="label">Loading plan type</label>
                                <div class="inline-group">
                                    <label class="radio">
                                        <input type="radio" ng-model="type" value="S">
                                        <i></i>STANDARD
                                    </label>
                                    <label class="radio">
                                        <input type="radio" ng-model="type" value="C">
                                        <i></i>COMBINED
                                    </label>
                                </div>
                            </section>
                            <div class="row">
                                <section class="col col-6">
                                    <label class="label">Factory</label>
                                    <label class="select">
                                        <select ng-model="data.factoryID" ng-options="factory.factoryID as factory.factoryUD for factory in factories">
                                            <option value=""></option>
                                        </select>
                                        <i></i>
                                    </label>
                                </section>
                                <section class="col col-6" ng-show="type=='S'">
                                    <label class="label">Booking</label>
                                    <label class="input">
                                        <i class="icon-append fa fa-search"></i>
                                        <input placeholder="Provide at least 3 chars to search for the booking" id="booking-autocomplete" type="text">
                                    </label>
                                    <div class="note">
                                        <strong>You selected</strong>:  <a style="font-size: 11px;" href="javascript:void(0)" data-toggle="modal" data-target="#bookingDetail">{{data.booking.bookingUD}}</a>
                                    </div>
                                </section>
                                <section class="col col-6" ng-show="type=='C'">
                                    <label class="label">Parent Loading Plan</label>
                                    <label class="input">
                                        <i class="icon-append fa fa-search"></i>
                                        <input placeholder="Provide the parent loading plan number (exact match)" id="parent-autocomplete" type="text">
                                    </label>
                                    <div class="note">
                                        <strong>You selected</strong>:  {{data.loadingPlan.loadingPlanUD}}
                                    </div>
                                </section>
                            </div>
                        </fieldset>
                        <footer>
                            <button class="btn btn-primary" type="button" ng-click="event.goNext()">
                                Next
                            </button>
                            <button class="btn btn-default" type="button" ng-click="event.goBack()">
                                Cancel
                            </button>
                        </footer>
                    </div>
                </div>
            </div>
        </div>
    </article>
</form>

<div id="bookingDetail" class="modal fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                    &times;
                </button>
                <h4 class="modal-title" id="myModalLabel">Booking: {{data.booking.bookingUD}}</h4>
            </div>
            <div class="modal-body no-padding">
                <div class="smart-form">
                    <fieldset>
                        <section>
                            <label class="label">Forwarder</label>
                            <label class="input">
                                <span class="form-control">{{data.booking.forwarderNM}}</span>
                            </label>
                        </section>
                        <div class="row">
                            <section class="col col-3">
                                <label class="label">Confirmation No</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.confirmationNo}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">B/L</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.blNo}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">Client</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.clientUD}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">Season</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.season}}</span>
                                </label>
                            </section>
                        </div>
                        <div class="row">
                            <section class="col col-3">
                                <label class="label">Port of loading</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.poLName}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">Port of discharge</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.podName}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">Feeder</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.feeder}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">Vessel</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.vessel}}</span>
                                </label>
                            </section>                            
                        </div>
                        <div class="row">
                            <section class="col col-3">
                                <label class="label">Cut off date</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.cutOffDate}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">ETD</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.etd}}</span>
                                </label>
                            </section>
                            <section class="col col-3">
                                <label class="label">ETA</label>
                                <label class="input">
                                    <span class="form-control">{{data.booking.eta}}</span>
                                </label>
                            </section>
                        </div>
                    </fieldset>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">
                    Close
                </button>
            </div>
        </div>
    </div>
</div>

@section pagejs {
    <script src="~/TemplateResource/js/plugin/jquery.autocomplete.min.js"></script>
    <script src="~/Angular/app/jsonService.js?c=@System.Guid.NewGuid().ToString().Replace("-", "")"></script>
    <script type="text/javascript">
        jsonService.serviceUrl = '@(System.Configuration.ConfigurationManager.AppSettings["BackendServiceUrl"])api/factoryloadingplan/';
        jsonService.supportServiceUrl = '@(System.Configuration.ConfigurationManager.AppSettings["BackendServiceUrl"])api/support/';
        jsonService.token = '@Session[Frontend.Customize.Common.ProjectDefinition.TOKEN_SESSION]';

        var context = {
            backURL: '@Url.Action("Index", "FactoryLoadingPlanMng")',
            nextURL: '@Url.Action("Edit", "FactoryLoadingPlanMng")/0?'
        }
    </script>
    <script src="~/Angular/app/factoryLoadingPlanMng/service.js?c=@System.Guid.NewGuid().ToString().Replace("-", "")"></script>
    <script src="~/Angular/app/factoryLoadingPlanMng/init.js?c=@System.Guid.NewGuid().ToString().Replace("-", "")"></script>
}

